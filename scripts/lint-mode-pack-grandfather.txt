# lint-mode-pack.sh grandfather list
#
# Format: <path>  <reason>  <target-date YYYY-MM-DD>
# Fields are whitespace-separated. Target date is inclusive — on/after that
# date, the lint blocks regardless of grandfather status.
#
# Entries land here only when an existing file violates MP1 (budget) or MP3
# (router cap) at the time the rule was introduced or tightened. New
# violations introduced after the rule landed are NOT eligible — fix them
# in the same commit.
#
# Cleanup arc: the #220-#226 cluster lands the structural prose cleanup that
# brings these files into compliance. Targets default to 2026-06-30 (end of
# that arc).
#
# Lines starting with `#` are comments. Blank lines are ignored.

# --- Mode pack token-budget overruns (MP1) ---

# --- Router cap overruns (MP3) ---
# (None at the time the rule landed — all routers are under the 80-line cap
# when blanks/comments are excluded, per the MP3 measurement rule.)
