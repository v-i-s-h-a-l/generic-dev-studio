# Local worklog for multi-session architecture arcs — session plans, audit
# partials, parking-lot entries, approved-decisions files. Outcomes graduate
# into ARCHITECTURE.md / ROADMAP.md / _shared/ primitives; the scratch trail
# stays local.
studio-consolidation/

.DS_Store

# Transient local handoff files for smoke-gated reviewer sessions.
.studio-runtime/

# Real release config belongs under ~/.dev-studio/<project>/config/. Keep only
# config/release.env.example in the repo.
config/release.env
