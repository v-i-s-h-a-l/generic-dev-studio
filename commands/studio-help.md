Open the Generic Dev Studio documentation page in the browser.

Steps:
1. Run `open "$HOME/.claude/skills/studio/docs.html"` to open the docs in the default browser
2. Tell the user: "Studio docs opened. You can also access them anytime at ~/.claude/skills/studio/docs.html"
