# Chained work plan for Host-agnostic Chanakya v2.
#
# Dry-run first:
#   scripts/studio-chain-runner.sh chains/host-agnostic-chanakya-v2.yaml --dry-run
#
# Sequence:
#   418 -> scope map and blocker boundary
#   419 -> host-aware dispatch adapter
#   420 -> helper extraction for repeatable jobs
#   421 -> mixed-host conformance and rollout
#   422 -> docs / portability cleanup after validation

schema_version: 1
chains:
  - name: host-agnostic-chanakya-v2
    base: main
    branch: feature/host-agnostic-chanakya-v2
    host: auto
    issues: [418, 419, 420, 421, 422]
