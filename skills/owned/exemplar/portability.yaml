schema_version: 1
hosts:
  - all
notes: |
  The exemplar uses only POSIX shell + a single model session. It runs on every
  host that satisfies the studio's worker capability floor. Add new hosts as
  they're adapted; the skill body is intentionally tool-dialect-neutral.
