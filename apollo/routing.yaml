schema_version: 1
name: apollo
invocation:
  slash_command: "/apollo"
  triggers:
    - "/apollo"
    - "/apollo memory"
    - "/apollo thermal"
    - "/apollo battery"
    - "/apollo cpu"
    - "/apollo network"
    - "/apollo network-efficiency"
    - "/apollo profile"
    - "/apollo measure <metric>"
    - "/apollo memory --capture-only"
    - "/apollo thermal --capture-only"
    - "/apollo battery --capture-only"
    - "/apollo cpu --capture-only"
    - "/apollo network --capture-only"
    - "/apollo measure network --capture-only"
    - "investigate memory regression"
    - "investigate thermal throttling"
    - "investigate battery drain"
    - "investigate CPU regression"
    - "investigate network regression"
    - "OOM crash"
    - "device heat"
    - "energy regression"
    - "high CPU"
    - "chatty network"
    - "high data usage"
    - "connection wakes"
    - "guided profiling session"
    - "run this flow on device"
    - "where were we on perf"
domains:
  - turnip-ios
  - performance
  - ios
