# git-workflow Summary

Load for git operations, branch hygiene, commits, pushes, or worktree setup.
Enforces owned-worktree edits, feature-branch commits, no direct base-branch
pushes, Studio GitHub wrapper usage, and user-owned bypasses for blocking gates.
