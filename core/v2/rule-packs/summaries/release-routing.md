# release-routing Summary

Load for release-manager work, tagging, TestFlight, App Store, Slack, signing,
or build/release messages. Follow `RELEASES.md` and release-manager contracts,
use configured notification routes, and never tag or publish without the
required release evidence.
